# Use Git LFS (Large File Storage) for the MaxMind GeoLite2 City database
# This prevents the large binary file from bloating the repo and ensures efficient storage/transfer.
data/GeoLite2-City.mmdb filter=lfs diff=lfs merge=lfs -text
