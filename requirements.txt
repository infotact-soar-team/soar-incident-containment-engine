# Web framework and ASGI server
fastapi==0.110.0              # Modern, fast web framework for building APIs
uvicorn==0.30.1               # ASGI server implementation for running FastAPI apps
starlette==0.36.3             # Lightweight ASGI toolkit (FastAPI is built on top of it)

# Data validation and settings management
pydantic==2.7.4               # Data validation and parsing using Python type hints
pydantic-settings==2.3.4      # Manage application settings with Pydantic models

# Database and ORM
SQLAlchemy==2.0.31            # SQL toolkit and ORM for database interactions
psycopg2-binary>=2.9.9        # PostgreSQL database driver (binary distribution)

# Environment and configuration
python-dotenv==1.0.1          # Load environment variables from .env files

# HTTP client
httpx<0.28                    # Async HTTP client for making external API calls

# File uploads
python-multipart>=0.0.7       # Handle multipart/form-data (file uploads) in FastAPI

# Testing
pytest==9.1.1                 # Testing framework for writing and running unit tests
