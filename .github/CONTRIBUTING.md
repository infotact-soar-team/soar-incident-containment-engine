# Contributing Guide

## Branch Naming
- `feature/<name>` — new functionality
- `bugfix/<name>` — bug fixes
- `docs/<name>` — documentation only
- `test/<name>` — test-only additions
- `perf/<name>` — performance improvements
- `chore/<name>` — tooling/repo maintenance

## Commit Message Format